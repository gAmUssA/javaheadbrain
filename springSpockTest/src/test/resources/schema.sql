drop table account if exists ;
create table account (id Integer primary key, balance double not null );

CREATE TABLE COMPANY(ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,COMPANY VARCHAR(255));
CREATE TABLE COMPANY_ASSOCIATE(ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,ASSOCIATE VARCHAR(255),COMPANY_ID INTEGER);